@page
@model IndexModel
@{
    ViewData["Title"] = "Homepage";
    ViewData["Description"] = "Welcome to the depression book";
    ViewData["Banner"] = "<img src=\"https://placehold.co/600x400\" width=\"100%\" alt=\"banner image\" />";
}

<img src="https://placehold.co/600x400" width="100%" alt="banner image" />
<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>
        Ich möchte dich herzlich willkommen heißen auf meinem Blog. Hier findest du alles rund um das Thema Depressionen und psychische Krankheiten. Ich selbst war Jahre lang selbst betroffen von Depressionen, Angststörungen und Panikattacken. Mittlerweile geht es mir wieder besser und ich möchte mit dir meine Erfahrungen teilen, um dir mit deiner eigenen Erkrankung zu helfen egal ob Betroffener, Angehöriger oder einfach nur Interessierter bist.

        Dich erwarten spannende Beiträge über meine eigene Krankheit, wie ich es wieder aus dem tiefen Loch heraus geschafft habe und den ein oder anderen Trick, um mit der Krankheit besser umgehen zu können. Des Weiteren soll es um Selbstentwicklung gehen und wie man dies mit der Krankheit in Einklang bringen kann.
    </p>
    <div>
        <h2>Recent posts:</h2>
        @foreach (PostPreviewModel recent in @Model.RecentPosts)
        {
            <section class="post-overview">
                <h2><a asp-page="./Post" asp-route-id="@recent.UrlIdentifier">@recent.Title</a></h2>
                <h4>Published at @recent.CreatedAt.ToString("MM/dd/yyyy")</h4>
                <section class="tag-section">
                    @foreach (var tag in recent.Tags)
                    {
                        <div class="tag">#@tag</div>
                    }
                </section>
                <p class="post-summary">
                    @recent.Summary
                </p>
            </section>
        }
    </div>
    <div class="target-audience">
        
    </div>
    <div>
        <h2>Most viewed: </h2>
        @foreach (MostViewedPostsModel mostViewed in @Model.MostViewedPosts)
        {
            <section class="post-overview">
                <h2><a asp-page="./Post" asp-route-id="@mostViewed.UrlIdentifier">@mostViewed.Title</a></h2>
                <p>Views: @mostViewed.ViewCount</p>
                <h4>Published at @mostViewed.CreatedAt.ToString("MM/dd/yyyy")</h4>
                <section class="tag-section">
                    @foreach (var tag in mostViewed.Tags)
                    {
                        <div class="tag">#@tag</div>
                    }
                </section>
                <p class="post-summary">
                    @mostViewed.Summary
                </p>
            </section>
        }
    </div>
</div>